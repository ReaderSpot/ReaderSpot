// Permite realizar búsquedas por diferentes criterios (título, autor, género), tambien por estado de lectura (no leido, leyendo, leido).
package main

import "fmt"

func busqueda_libros_func() {
	fmt.Print("Modulo busqueda libros")
}
